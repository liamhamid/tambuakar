# Kebergantungan runtime untuk Tambuakar collector (Layer A).
# Teras v1 = pustaka standard sahaja (Wikidata, GDELT). Baris di bawah untuk
# sumber yang perlukan pihak ketiga:
pytrends>=4.9   # Google Trends (minat audiens) — import dilindungi dalam adapter
urllib3<2       # pytrends 4.9 guna Retry(method_whitelist=…) yang dibuang di urllib3 2.x
fastapi>=0.110  # Serving API (dedah Gold read-only)
uvicorn>=0.29   # server ASGI untuk Serving API
